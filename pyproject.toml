[project]
name = "sab-top-tournaments-items-extractor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "loguru",
    "pydantic",
    "pydantic-settings",
    "selenium",
    "google-api-python-client",
    "google-auth",
]
