# Project Structure

- `src/` — functional submodules (business logic, features)
- `lib/` — utilities, infrastructure helpers (browser, clients, wrappers)
- `config/` — configuration files per module
- Never put infrastructure/utility code in `src/`
